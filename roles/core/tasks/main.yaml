---
- name: Run apt upgrade
  apt:
    upgrade: yes
    update_cache: yes
    cache_valid_time: 7200

- name: Ensure pip3 is installed
  apt:
    name: python3-pip
    state: present
    update_cache: yes
  become: true

